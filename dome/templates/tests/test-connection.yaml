apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dome.fullname" . }}-test-connection"
  labels:
    {{- include "dome.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dome.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
